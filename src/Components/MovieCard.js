import React from 'react';

const MovieCard = (props) => {
    return ( 
        <div>
            
          <p> {props.item.title} </p>
    
    
        </div>
     );
}
 
export default MovieCard;